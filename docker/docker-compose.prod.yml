version: "3.7"

services:
  frontend:
    build:
      context: ./../src/frontend/main
      dockerfile: Dockerfile-production

  backend:
    build:
      context: ./../src/backend/main
      dockerfile: Dockerfile-production
